

if [ `id -u` -ne 0 ]; then
	\echo "You need to be root to execute this script"
	\echo "Run 'sudo ./.install' or type 'make' instead"
	\exit 1
fi

cd validator
make
mv validate /usr/bin
cd ..


cd stl2g
make
cd ..
